# 리팩토링 문서

## 1. 개요

이 문서는 REST 서버 애플리케이션의 리팩토링 과정에서 수행된 변경 사항과 그 이유를 설명합니다. 주요 목표는 성능 향상, 안정성 개선, 그리고 유지보수성 강화입니다.

## 2. 트랜잭션 관리 개선

### 2.1. DTO-Entity 변환 과정에서의 트랜잭션 중첩 문제 해결

#### 문제 상황
- `AnnuityLottoDto.toEntity()` 및 `LottoDto.toEntity()` 메서드 내에서 `transaction` 블록을 사용했으며, 이로 인해 서비스 계층에서 이미 시작된 트랜잭션과 중첩 발생
- 중첩된 트랜잭션으로 인해 "Connection is closed" 오류가 발생하여 데이터 저장 실패

#### 개선 내용
1. **DTO에서 데이터베이스 액세스 로직 제거**
   - `toEntity()` 메서드에서 transaction 블록과 데이터베이스 조회 로직 제거
   - 새 엔티티 생성만 담당하는 간단한 메서드로 변경

2. **책임 분리**
   - `updateEntity()` 메서드 추가하여 기존 엔티티 업데이트 로직 분리
   - 데이터베이스 조회/갱신 로직을 서비스 계층으로 이동하여 명확한 책임 분리

3. **트랜잭션 관리 개선**
   - 서비스 레이어에서 코루틴에 최적화된 `dbQuery` 블록 사용
   - 모든 데이터베이스 작업을 단일 트랜잭션 내에서 처리
   - 개별 엔티티 처리 시 예외를 포착하여 특정 항목 실패해도 전체 작업 계속 가능

### 2.2. 일반 로또 서비스 리팩토링

기존 연금복권과 동일한 문제가 잠재되어 있어 동일한 패턴으로 리팩토링을 수행했습니다:
- `LottoDto.toEntity()` 메서드에서 트랜잭션 및 DB 조회 로직 제거
- `LottoService.putLotto()` 메서드에서 트랜잭션 관리 개선

## 3. 외부 API 연결 처리 개선

### 3.1. 연결 안정성 문제 해결

#### 문제 상황
- 외부 로또 API (`www.dhlottery.co.kr`) 연결 시 "connection timed out after 30000 ms" 오류 발생
- 병렬 처리 방식으로 인한 많은 동시 요청으로 서버 부하 및 연결 거부 발생

#### 처음 시도한 개선 및 추가 문제 발견
처음에는 타임아웃 시간을 **줄이는** 방향으로 접근했으나, 이는 오히려 문제를 악화시켰습니다:
- 30초 → 10~15초로 타임아웃 시간 감소
- 서버가 느린 경우 짧은 타임아웃 시간으로는 응답을 받기 충분하지 않음
- 재시도가 계속 발생하며 결국 실패

#### 최종 개선 내용
1. **타임아웃 설정 최적화**
   - 응답 타임아웃: 15초 → 60초로 증가
   - 연결 타임아웃: 10초 → 30초로 증가
   - 연결 유휴 시간: 30초 → 60초로 증가
   - 연결 수명: 5분 → 10분으로 증가

2. **WebClient 설정 최적화**
   - 커스텀 `ConnectionProvider` 설정으로 연결 풀 관리 개선
   - 최대 동시 연결 수 제한으로 서버 부하 감소

3. **재시도 간격 및 요청 지연 최적화**
   - 초기 재시도 간격: 1초 → 2초로 증가
   - 요청 사이 지연: 로또 API 500ms → 1000ms로 증가
   - 연금복권 API 1000ms → 2000ms로 증가

4. **JVM 타겟 설정 문제 해결**
   - JVM 타겟 버전을 명시적으로 21로 설정하여 바이트코드 호환성 오류 해결
   - `jvmToolchain(21)` 설정을 통해 Kotlin 컴파일러에 JVM 타겟 명시

### 3.2. Jsoup을 사용한 웹 스크래핑 개선 (연금복권)

- 타임아웃 설정 증가 (5초 → 10초)
- 연결 실패 시 예외를 호출자에게 전파하지 않고 null 반환하도록 변경
- 재시도 메커니즘 적용하여 안정성 향상

## 4. 코드 품질 개선

### 4.1. 예외 처리 강화
- 개별 항목 처리 실패 시에도 전체 배치가 계속 진행되도록 예외 처리 로직 추가
- 상세한 로깅을 통해 문제 추적 용이성 개선

### 4.2. 책임 분리 명확화
- DTO: 데이터 변환만 담당
- 서비스: 비즈니스 로직 및 트랜잭션 관리 담당
- 리포지토리: 데이터 액세스 담당

## 5. 성능 개선

### 5.1. 최적화된 트랜잭션 관리
- 중첩 트랜잭션 제거로 데이터베이스 연결 관리 오버헤드 감소
- 코루틴 기반 트랜잭션 처리로 효율적인 비동기 처리
- 배치 처리 중 일부 항목 실패 시에도 전체 작업 계속 진행 가능

### 5.2. 효율적인 데이터베이스 액세스
- 단일 트랜잭션 내에서 여러 항목 처리로 커넥션 사용 효율성 향상
- 불필요한 트랜잭션 경계 제거로 성능 향상

### 5.3. 외부 API 요청 처리 최적화
- 병렬 처리는 많은 연결을 동시에 소비하여 서버 과부하 및 연결 거부 발생 가능
- 순차적 처리로 변경하여 안정성 우선
- 비동기 처리의 이점이 큰 작업만 비동기로 처리하고, 나머지는 동기적으로 처리하는 균형 접근법 채택

## 6. 향후 고려 사항

### 6.1. 트랜잭션 관리 추가 개선
- 대규모 데이터 배치 처리를 위한 청크 기반 처리 도입 고려
- 트랜잭션 격리 수준 최적화 검토

### 6.2. 연결 풀 설정 최적화
- HikariCP 연결 풀 설정 조정 고려 (연결 유지 시간, 최대 풀 크기 등)
- 연결 재사용 전략 검토

### 6.3. 외부 API 요청 전략
- 주기적 데이터 사전 동기화 전략 검토 (배치 작업으로 데이터 미리 수집)
- 외부 API 장애 시 대체 데이터 소스 또는 캐싱 전략 고려

## 7. 결론

이번 리팩토링을 통해 애플리케이션의 안정성과 성능이 크게 향상되었습니다. 특히 트랜잭션 관리 개선으로 "Connection is closed" 오류가 해결되었고, 외부 API 연결 처리 개선으로 "connection timed out" 오류가 해결되었습니다. 코드의 책임 분리가 명확해짐에 따라 유지보수성도 개선되었습니다. 